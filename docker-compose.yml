version: '3'

services:
  db:
    image: 'mongo'
    container_name: mongodb
    ports:
      - '27017:27017'
    volumes:
      - ./data/db:/data/db

  redis:
    image: "redis:alpine"

    command: redis-server --requirepass my_redis_secret

    ports:
      - "6379:6379"
